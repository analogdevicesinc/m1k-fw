language: c
sudo: required

matrix:
  include:
    - compiler: "gcc"
      os: linux
      env:
        - BUILD_TYPE=m1k_make

addons:
  artifacts: true

script:
  - ./ci/travis/run_build

notifications:
  email:
    on_success: change
    on_failure: always
